{
  "name": "relic-subgraph",
  "version": "0.1.0",
  "description": "The Graph subgraph for Infinite Relic",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio relic",
    "create-local": "graph create --node http://localhost:8020/ relic",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 relic"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.69.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
